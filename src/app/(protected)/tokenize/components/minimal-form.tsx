'use client';

import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export default function MinimalForm() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Minimal Form</CardTitle>
      </CardHeader>
      <CardContent>
        <p>This is a minimal working example.</p>
      </CardContent>
    </Card>
  );
}
